name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  ci:
    uses: Truthdb/.github/.github/workflows/docker-image-ci.yml@workflows-v1
    with:
      image_repo: ghcr.io/truthdb/truthdb-installer-kernel-builder-image
