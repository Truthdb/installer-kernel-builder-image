name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  ci:
    uses: Truthdb/.github/.github/workflows/docker-image-ci.yml@issue-17-workflows
    with:
      image_repo: ghcr.io/truthdb/truthdb-installer-kernel-builder-image
