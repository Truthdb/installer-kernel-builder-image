# installer-kernel-builder-image
Produces a docker image for building the linux kernel used in the truthdb installer.

# valid branch names
Only valid branch names are feature/* or fix/*. Will be enforced in pullrequests.
